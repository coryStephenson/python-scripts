cars = ['bmw', 'audi', 'toyota', 'subaru']

# The print() function is required to display the output of the len() function in a script
# In interactive mode, len(cars) will suffice.

print(len(cars))
